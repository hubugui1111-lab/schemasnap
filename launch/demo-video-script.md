# 45-second demo video script

1. Show `baseline.csv` and run `schemasnap snapshot` (5 seconds).
2. Open the small JSON snapshot and point out that no category labels or rows are present (8 seconds).
3. Run the all-failures diff and pause on the 3 BREAKING / 3 WARNING / 1 INFO summary (12 seconds).
4. Open the Markdown report in a pull-request-like preview (8 seconds).
5. Run the timezone-loss fixture and the restricted DuckDB SQL example (8 seconds).
6. Close on “local, deterministic, Apache-2.0” and the repository URL (4 seconds).

Record a fresh terminal session from the tagged release. Do not animate invented output or quote star
counts/adoption claims. Add English captions and a concise Chinese subtitle track.

