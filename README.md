# SchemaSnap

SchemaSnap is a privacy-first, Git-native data contract snapshot and diff CLI.

> Development checkpoint: the executable implementation follows the committed RED contracts.

The Python distribution is **`schemasnap-data`**. The unrelated `schemasnap` package on PyPI is
not this project. The installed import package and CLI are both `schemasnap`.

